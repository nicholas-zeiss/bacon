/**
 * This is controller is responsible for the Home view which occurs the app is waiting for the user to 
 * search for an actor or there was an error.
 */

function HomeController() {
	
}

export default HomeController;

